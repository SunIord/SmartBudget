{
    "version": "0.2.0",
    "tasks": [
        {
            "label": "Executar SmartBudget",
            "type": "shell",
            "command": "cmake -B build -G \"MinGW Makefiles\" && cmake --build build",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
