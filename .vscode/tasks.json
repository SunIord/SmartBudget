{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compilar SmartBudget",
            "type": "shell",
            "command": "cmake -B build -G \"MinGW Makefiles\" && cmake --build build",
            "group": "build",
            "problemMatcher": []
        }
    ]
}
